  <% disabled = defined?(disabled) && disabled %>
  <%= texto _("Emisor factura"), 'gasto', 'proveedor_nombre', '1', value: obj_value, autocomplete: true, disabled: disabled,
        after_update: "function(element,value){" + remote_function( url: { action: :completa_proveedor_nif }, with: "'search='+element.value") + "}" %>
