  <% disabled = defined?(disabled) && disabled %>
  <div id="presupuesto_pais">
    <!-- De momento permitimos gastos desde agentes en paises distintos del propio... confirmar -->
    <%= selector _("País"), 'presupuesto', 'pais_id', @paises, false, "1", {selected: pais_id, obligatorio: true, disabled: disabled} %>
  </div>
